[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --cov=agents
    --cov=tools
    --cov=core
    --cov-report=term-missing
    --cov-fail-under=70
asyncio_mode = auto
